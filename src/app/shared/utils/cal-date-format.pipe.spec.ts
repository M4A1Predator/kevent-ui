import { CalDateFormatPipe } from './cal-date-format.pipe';

describe('CalDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CalDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
